package ${packageName};

import ${rootName}.framework.BasePresenterApi;
import ${rootName}.framework.BaseViewApi;

public class ${name}Contract {

    interface View extends BaseViewApi<Presenter> {

    }

    interface Presenter extends BasePresenterApi{

    }
}
